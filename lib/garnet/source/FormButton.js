(function (enyo, g, scope) {
	/**
	* _g.FormButton_ extends {@link g.Button} control with specific width, height and margin attributes of CSS for a form control.
	*
	* @class g.FormButton
	* @extends g.Button
	* @public
	*/
	enyo.kind(
		/** @lends g.Button.prototype */ {

		/**
		* @private
		*/
		name: "g.FormButton",

		/**
		* @private
		*/
		kind: "g.Button",


		/**
		* @private
		*/
		classes: "g-form-button"
	});

})(enyo, g, this);
