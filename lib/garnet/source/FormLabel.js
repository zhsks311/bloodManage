(function (enyo, g, scope) {
	/**
	* _g.FormLabel_ is a control which provides styling (e.g margin, font color and font size)
	* to a label in a form.
	*
	* @class g.FormLabel
	* @extends enyo.Control
	* @public
	*/
	enyo.kind(
		/** @lends g.FormLabel.prototype */ {

		/**
		* @private
		*/
		name: "g.FormLabel",

		/**
		* @private
		*/
		classes: "g-form-label"
	});

})(enyo, g, this);
